services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: oppgaveflyt
    ports:
      - "5432:5432"
    volumes:
      - oppgaveflyt-db:/var/lib/postgresql/data

volumes:
  oppgaveflyt-db:
